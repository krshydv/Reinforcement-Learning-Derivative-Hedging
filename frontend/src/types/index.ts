export type MarketPoint = { t: number; price: number; volatility: number }
export type MetricSeries = { index: number; value: number }
export type PortfolioPosition = { symbol: string; quantity: number; avgPrice: number; delta: number; gamma: number }
