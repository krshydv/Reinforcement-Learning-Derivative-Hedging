apiVersion: v1
kind: Secret
metadata:
  name: rl-platform-secrets
  namespace: rl-platform
type: Opaque
stringData:
  SECRET_KEY: ${SECRET_KEY}
  POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
  WAND_B_API_KEY: ${WAND_B_API_KEY}
  GRAFANA_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD}
